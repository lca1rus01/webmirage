"""Platform plugins for webmirage.

Each platform is a self-contained module under this package.
Add a new platform by creating a new directory with a ``tools.py``
that exposes ``register_tools(server)`` and ``is_available()``.
"""
